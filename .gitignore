# Binary (root-level compiled binary only, not cmd/neomd/ source dir)
/neomd
/neomd-freebsd
/neomd-android

# Go build cache
*.test
*.out

# Config with real credentials (use config.toml.example instead)
config.toml

# Local dev overrides (test recipients, etc.) — never commit
.env

# Editor temp files (compose buffers)
neomd-*.md

# OS
.DS_Store
.codex

.claude/

# Verification output
evidence/

# The repository's verify skill is intentionally shared with Claude harnesses.
!.claude/
!.claude/skills/
!.claude/skills/verify/
!.claude/skills/verify/SKILL.md
